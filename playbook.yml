- hosts: localhost
  tasks:
    - name : Deploy the Service 
      k8s:
        state: present  
        definition: ""
        validate_certs: no
        namespace: default
        #src: service.yml

    - name : Deploy the app 
      k8s:
        state: present  
        definition: ""
        validate_certs: no
        namespace: default        